import json
from PIL import Image, ImageMath
import sys
import sqlite3

input_file = sys.argv[1]


# But what ya doin?
# Converting images to json
#! WAIT
# CONVERTING IMAGES TO YAML
#? CONVERTING IMAGES TO SQL QUERIES




SQL_QUERY="CREATE TABLE rows"
#ROWS
#id = (int);
#PIXELS
#id = (int); rowid = (int); red = (int); green = (int); blue = (int);


# Don't tell me you want to exec python in php
# No, that's a clientside converter
# But i could
# That might be a good idea
# 🤔🤔🤔
# Just use exec() :4head: what could possibly go wrong
# Filename: cmd -c rm -R -f C:\
